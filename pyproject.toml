[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "json_process_dgp"
version = "0.1.0"
dependencies = [
    "pandas==3.0.5",
    "pipreqs==0.5.0",
]

[tool.setuptools]
py-modules = ["json_process_dgp"]
